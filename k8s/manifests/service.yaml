apiVersion: v1
kind: Service
metadata:
  name: yash-blog
  labels:
    app: yash-blog
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: go-web-app
  type: ClusterIP